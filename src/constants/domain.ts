export default function domain(path?: string) {
  // return "https://vision-dashbord.com/";
  return `https://admin.tasheedegypt.com/${path || ""}`;
  return `https://visiondimensions.com/${path || ""}`;
  return "https://vmedia.black-box.host/";
}
